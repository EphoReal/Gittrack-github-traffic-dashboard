__pycache__/
*.py[cod]
.DS_Store

# ============================================================
# 隐私：真实流量数据 —— 公开仓库绝不能提交以下内容
# ============================================================

# 累积的历史流量：含仓库名、每日 views/clones 精确数字、独立访客数、
# 以及累积后的访客来源(referrers)与热门路径(paths)。
# 本仓库是「工具仓库」，历史数据应放在你自己的私有数据仓库里。
data/traffic-history.json
data/*.tmp

# GitHub 后台导出的 Traffic CSV（同样含真实流量，仅供 seed_from_csv.py 一次性回填）
*.csv

# 本地 AI 会话/记忆目录（可能记录真实用户名与仓库名）
.workbuddy-ai/
